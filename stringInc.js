function incrementString(strng) {
  // return incrementedString
  let letters = strng.split("").filter((el) => Number(el));
  let digits = string.split("").filter((el) => Number(el));

  return string;
}

//   BigInt(strng.split('').filter(el=>Number(el).join('')))++
console.log(incrementString("foo"));
